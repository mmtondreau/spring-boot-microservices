server:
  port: 8081
spring:
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
      name: "ChildOne"
  config:
    import: optional:configserver:http://localhost:8888/
  application:
    name: "ChildOne"
